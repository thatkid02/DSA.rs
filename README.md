# DSA in C++

Data Structures and Algorithms in modern C++ (C++20) with Clang.

## Quick Start

```bash
cmake -B build && cmake --build build && ./build/bin/dsa
```

## VS Code Formatting

✅ **Auto-format on save** is enabled  
📝 **Manual format shortcut:** `Shift + Option + F` (macOS)

**Not working?**

1. Reload VS Code: `Cmd+Shift+P` → "Reload Window"
2. Or manually: `clang-format -i src/code150/file.cpp`
3. Check your file is `.cpp` extension

## Adding Problems

Create `src/code150/problem.cpp` and include in `main.cpp`. That's it!
